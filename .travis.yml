language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-assemble-skeletor' ]; then cd .. && eval "mv $currentfolder generator-assemble-skeletor" && cd generator-assemble-skeletor; fi
